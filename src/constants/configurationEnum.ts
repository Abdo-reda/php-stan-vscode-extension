export enum ExtensionConfigurations {
    LEVEL = "level",
    ANALYSIS_ON = "analysisOn",
    ANALYSIS_SCOPE = "analysisScope"
}

export enum AnalysisOn {
    MANUAL = "manual",
    ON_SAVE = "onSave",
    ON_CHANGE = "onChange"
}

export enum AnalysisScope {
    FILE = "file",
    DIRECTORY = "directory",
    WORKSPACE = "workspace"
}